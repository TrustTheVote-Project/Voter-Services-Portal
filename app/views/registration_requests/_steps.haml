.row#steps
  %div{ 'data-bind' => "attr: { class: stepClass(currentPageIdx(), 0) }" }
    %h2 Eligibility

  %div{ 'data-bind' => "attr: { class: stepClass(currentPageIdx(), 1) }" }
    %h2 Identity

  %div{ 'data-bind' => "attr: { class: stepClass(currentPageIdx(), 2) }" }
    %h2 Addresses

  %div{ 'data-bind' => "attr: { class: stepClass(currentPageIdx(), 3) }" }
    %h2 Options

  %div{ 'data-bind' => "attr: { class: stepClass(currentPageIdx(), [ 4, 5, 6 ], 'span2 last') }" }
    %h2 Complete &amp; Mail
