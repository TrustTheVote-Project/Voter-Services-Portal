- reg = RegistrationDetailsPresenter.new(@registration)
= render partial: "search/steps", locals: { step: 'view' }

.row
  .padded_page_span.section
    .head
      %h3= reg.full_name
      .description
        Update your Virginia Voter Record.
        If this is not you, please go back to the identity page.

    %p
      Please update any information that has changed. Doint so will generate
      a form PDF that must be printed, signed and mailed. The envelope needs
      to be postmarked by September 6, 2012 to take effect for the next election.


    .span10.summary
      .head
        %p
          %strong Registration Status: #{reg.registration_status}
        %p Next Election Date: June 12, 2012

      .body
        %table.table.properties
          %tr
            %th Name:
            %td= reg.full_name
          %tr
            %th Date of Birth:
            %td= reg.dob
          %tr
            %th Gender:
            %td= reg.gender
          %tr
            %th Social Security Number:
            %td= reg.ssn
          %tr
            %th Phone:
            %td= reg.phone
          %tr
            %th Email:
            %td= reg.email
          %tr
            %th Registration Address:
            %td= reg.voting_address
          %tr
            %th Mailing Address:
            %td= reg.mailing_address
          %tr
            %th Political Party Preference:
            %td= reg.party_affiliation


      .form-actions
        = button_tag "Update Information &rarr;".html_safe, class: 'next btn', 'data-bind' => 'css: { disabled: invalid }'
