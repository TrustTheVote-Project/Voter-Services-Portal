- registered_with_dmv = @submission_success && @voter_id.present? && @registration.dmv_id.present?
- @page_id = registered_with_dmv ? 'nr_unregistered_dmv' : 'nr_download'

= render partial: 'steps'

.row.disclaimer
  .span12
    %p
      All information in this online experience is required by state or federal
      law, unless specifically marked optional.

.row
  .padded_page_span
    - if @registration.submission_failed
      %p TBD 310 error

    - if registered_with_dmv
      = render partial: 'unregistered_dmv'
    - else
      = render partial: 'download'
      = render partial: 'congratulations'

