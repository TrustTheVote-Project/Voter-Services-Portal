.padded_page_span.section#overview
  .head
    %h3= raw t("ballot_info.title")
    .description= raw t("ballot_info.subtitle")

  .row
    %p
      #{raw t("ballot_info.header.title")}<br/>
      #{@info.election_date}<br/>
      #{@info.election_name}<br/>
      #{@info.locality} #{raw t("ballot_info.header.precinct")} #{@info.precinct}

  - @info.contests.each do |contest|
    .row
      = render partial: contest[:type].downcase, locals: { contest: contest }

  .row.form-actions
    = link_to "Done", :registration, class: 'done bt'
